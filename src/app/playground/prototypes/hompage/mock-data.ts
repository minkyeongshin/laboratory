/**
 * Mock data for hompage prototype.
 * Import types from @/types/ rather than re-declaring.
 */

// Example: import { Network } from "@/types/types";

export const mockData = {
  // Add mock data here
};
